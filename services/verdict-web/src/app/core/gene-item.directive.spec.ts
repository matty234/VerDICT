import { GeneItemDirective } from './gene-item.directive';

describe('GeneItemDirective', () => {
	it('should create an instance', () => {
		const directive = new GeneItemDirective();
		expect(directive).toBeTruthy();
	});
});
