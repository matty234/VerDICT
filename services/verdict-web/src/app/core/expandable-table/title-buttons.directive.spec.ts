import { TitleButtonsDirective } from './title-buttons.directive';

describe('TitleButtonsDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleButtonsDirective();
    expect(directive).toBeTruthy();
  });
});
