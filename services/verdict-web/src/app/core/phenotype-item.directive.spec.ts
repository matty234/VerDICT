import { PhenotypeItemDirective } from './phenotype-item.directive';

describe('PhenotypeItemDirective', () => {
	it('should create an instance', () => {
		const directive = new PhenotypeItemDirective();
		expect(directive).toBeTruthy();
	});
});
